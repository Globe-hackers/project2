<div class="container-sm" id="tip-list">
    <h1>Discover all the amazing stories of our members</h1>
    <a href="/tips/create" class="btn btn-sm btn-outline-dark">Share your own story</a>
    <div class="row">
        {{#each tipsArr}}
        <div class="col-md-6 col-lg-3" id="card-area">
            <div class="card">
                <img class="card-img-top" src="{{this.imageUrl}}" alt="story image">
                <div class="card-body">
                    <h5 class="title">{{this.title}}</h5>
                    <h6 class="country">{{this.country}}</h6>
                    <h6 class="city">{{this.city}}</h6>
                    <p class="description">Description: {{this.description}}</p>
                    <p class="category">Categories: {{category}}</p>
                    <a href="/tips/tip/{{this._id}}" class="btn btn-sm btn-outline-dark">View details</a>
                    {{#if user}}
                    <form action="/tips/{{this._id}}/delete" method="POST" id="delete-button">
                        <button class="btn btn-sm btn-outline-dark">Delete</button>
                    </form>
                    {{/if}}
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>